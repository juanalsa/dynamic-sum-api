server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dynamicsum
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  cache:
    type: simple

logging:
  level:
    root: INFO

